kind = "web"
previewPath = "/"
title = "Poupar Agro Comercial"
version = "1.0.0"
id = "artifacts/poupar-agro"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/poupar-agro run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/poupar-agro", "run", "build" ]
publicDir = "artifacts/poupar-agro/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
